name: Publish Saya release
on:
  schedule:
    - cron: '30 20 31 8 *'
  workflow_dispatch: {}

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release edit saya-v1.2.6 --repo DivanDoge/locus-team --draft=false